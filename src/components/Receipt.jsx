import React from 'react';
import Card from './Card';

const Receipt = ({receiverName, bank, accountNum, amount, date, narration,  }) => {
    return (
        <Card>
            
        </Card>
    );
}

export default Receipt;